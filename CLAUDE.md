## 公開・コミット前チェックルール

public リポジトリなど外部に出るものをコミット・公開する前に、対象ファイル全体へ以下を毎回確認する:

1. **秘密情報**: `password|token|secret|api[_-]?key|credential` を grep してヒットなし。
2. **個人・マシン固有情報**: ユーザー名入りパス（`C:\Users\<名前>`、`/c/Users/<名前>`、`/Users/<名前>`）、
   個人・仕事のメールアドレス、ホスト名を grep してヒットなし。
   この環境で実際に検出すべき具体的キーワードは、各リポジトリの `CLAUDE.local.md` に定義する
   （キーワード自体が個人情報なので公開ファイルには書かない）。
3. **パスの書き方**: ハードコードせず `$env:USERPROFILE`（PowerShell）や `~` / `$HOME`（bash）で書く。
4. **履歴**: 上記が一度でもコミットに入った場合、ファイル修正だけでは不十分。
   履歴の書き換え（orphan コミット化）またはリポジトリの削除・再作成で対応する
   （force push では旧コミットがハッシュ指定で GitHub に残り得る）。
